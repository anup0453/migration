export enum RoleEnum {
  assistant = 'assistant',
  user = 'user',
  system = 'system',
  tool = 'tool',
}

export enum LoadBalancerRegionEnum {
  EMEA = 'emea',
  AMERICAS = 'americas',
  APAC = 'apac',
  WORLDWIDE = 'worldwide',
  NONE = 'none',
}

export enum ModelTypeEnum {
  EMBEDDING = 'embedding',
  LLM = 'llm',
}

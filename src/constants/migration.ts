export enum MigrationDocumentType {
  AI_SEARCH = 'AI SEARCH',
  DATABASE = 'DATABASE',
}

export enum MigrationDocumentStatus {
  RUNNING = 'running',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
